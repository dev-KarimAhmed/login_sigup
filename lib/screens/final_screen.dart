
import 'package:flutter/material.dart';

class FinalScreen extends StatelessWidget {
  const FinalScreen({Key? key}) : super(key: key);
 static String id = 'FinalScreen';
  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
